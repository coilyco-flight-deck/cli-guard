# Catalog metadata for the cross-repo knowledge graph. Dev verbs are justfile recipes.
# This path survives because check_catalog_block and catalog-trifecta pin it upstream.

catalog:
  description: "Security framework for guarded command-line tools with scoped authority, validation, and audit logs. The generic engine Ward and ward-mcp build against."
  dependsOn: []
