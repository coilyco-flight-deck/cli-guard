// Wildcard proving-slice Guardfile: a readonly forgejo build, default-deny for writes.
// What the wildcards mount and why `never delete` is there: docs/specverb-policy.md.

wrap ward ops forgejo {
    spec forgejo.swagger.v1.json
    base-url "https://forgejo.coilysiren.me/api/v1"
    auth header-token {
        header Authorization
        prefix "token "
        value ssm "/forgejo/api-token"
    }

    can get "*"
    can list "*"
    never delete "*" {
        message "this build is read-only; deletes are denied fleet-wide"
    }
}
